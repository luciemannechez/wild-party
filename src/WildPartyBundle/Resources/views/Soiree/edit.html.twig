{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Soiree edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('wild_party_homepage') }}" class="btn btn-primary">
            Retourner à la liste des soirées
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
